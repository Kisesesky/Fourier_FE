# 2025-11-18 Summary

- 모든 도메인 UI(채팅, Docs, Issues, Members, Worksheet, Calendar, Dashboard)를 `app/(workspace)/workspace/[teamId]/[projectId]/<domain>/_components|_model|_service` 구조로 옮기고 `@/workspace/<domain>/*` alias로 통일했다.
- 워크스페이스 홈(팀/프로젝트) 관련 컴포넌트도 `@/workspace/root/*` 아래로 이동하여 `/`와 `/workspace/[team]/[project]`가 동일한 컴포넌트를 공유한다.
- legacy `setup/workspace`와 `WorkspaceWizard`를 삭제하고 Sidebar의 New Workspace CTA도 제거했다.
- README에 새 구조와 alias를 명시하고 `npm run build`로 전체 검증 완료.
